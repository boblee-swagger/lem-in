-[] read the given file as argument
-[] check file's validiy
    -[] if #number_of_ants, #starts and #ends are set
        -[] pick the number number_of_ants from the first line (it should be a number between 1 and 1000)
        -[] pick the first line after #start and set it as starting room  and ending room
    -[] check room format's
    -[] check for duplicate rooms
    -[] check links validiy
    -[] if there a link between #start room and #end room 
-[] throw error if file not valid
-[] create tunnels and rooms 

        
